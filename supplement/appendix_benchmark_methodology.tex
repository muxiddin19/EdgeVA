%%
%% appendix_benchmark_methodology.tex
%% Online Supplementary Material — compile with:
%%   pdflatex appendix_benchmark_methodology.tex
%% No external bib file needed (all refs inline).
%%

\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.5cm]{geometry}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage{microtype}
\hypersetup{colorlinks=true, urlcolor=blue, linkcolor=black}

\title{\textbf{Online Supplementary Material}\\[4pt]
  \large EdgeVA Benchmarking Methodology\\[2pt]
  \normalsize For: ``Edge-Intelligent Video Analytics: A Systematic Survey
  of Architectures, Applications, and Deployment Strategies
  Across Smart City Verticals''\\
  \textit{ACM Computing Surveys}}

\author{Mukhiddin Toshpulatov\,\textsuperscript{1,2} \and
        Wookey Lee\,\textsuperscript{3} \and
        Jinsoo Cho\,\textsuperscript{1}}

\date{%
  \textsuperscript{1}Gachon University, Seongnam, South Korea \quad
  \textsuperscript{2}Jizzakh Branch of NUUz, Uzbekistan\\
  \textsuperscript{3}Inha University, Incheon, South Korea\\[4pt]
  Corresponding: \href{mailto:trinity@inha.ac.kr}{trinity@inha.ac.kr},
  \href{mailto:jscho@gachon.ac.kr}{jscho@gachon.ac.kr}\\[4pt]
  Artefacts: \url{https://github.com/muxiddin19/EdgeVA}}

\begin{document}
\maketitle
\tableofcontents
\bigskip

\noindent\textbf{Purpose.}
This document provides the complete measurement protocol for all
latency, throughput, and pipeline-timing figures in the main article
(Table~2, Figure~5, and Section~6.4). All scripts, model weights,
environment specifications, and raw result CSVs are available at the
repository above.

%% ---------------------------------------------------------------
\section{Hardware Configuration}

\subsection{Server Platform (CPU and GPU Runs)}

\begin{itemize}[leftmargin=*, noitemsep]
  \item \textbf{CPU:} Intel Core i9-14900K (24-core/32-thread,
    3.2\,GHz base / 6.0\,GHz boost)
  \item \textbf{GPU:} Dual NVIDIA GeForce RTX\,4090 (24\,GB GDDR6X each);
    benchmarks pinned to GPU\,0 (\texttt{device\_id=0})
  \item \textbf{RAM:} 125\,GB DDR5
  \item \textbf{OS:} Ubuntu\,22.04.4 LTS (kernel 6.8.0)
  \item \textbf{Driver / CUDA:} NVIDIA driver 535.288.01; CUDA\,12.2
  \item \textbf{Conda environment:} isolated \texttt{edgeva} env
    (Python\,3.10.20); key packages:
    \texttt{onnxruntime-gpu}\,1.24.4,
    \texttt{ultralytics}\,8.4.36,
    \texttt{onnxslim}\,0.1.90,
    \texttt{onnx}\,1.16.0,
    \texttt{scipy}\,1.13.0,
    \texttt{numpy}\,1.26.4
\end{itemize}

\subsection{Jetson Orin NX Figures (from Literature)}

Jetson Orin NX 16\,GB results are sourced from Alqahtani et al.\
(2024): JetPack\,5.1.2, TensorRT\,8.6.2, ONNX\,1.14, INT8
calibration on 500 representative images from COCO 2017 val.

%% ---------------------------------------------------------------
\section{Model Export Procedure}

ONNX models were exported with the \texttt{ultralytics} Python API:

\begin{verbatim}
from ultralytics import YOLO
model = YOLO("yolov8n.pt")          # or yolov8s.pt, yolov8m.pt
model.export(format="onnx",
             opset=20,
             simplify=True,
             dynamic=False,
             imgsz=640)
\end{verbatim}

Post-processed with \texttt{onnxslim}\,0.1.90 for graph simplification.
No TensorRT engine conversion was performed for server-side runs;
the ONNX Runtime \texttt{CUDAExecutionProvider} with
\texttt{ORT\_ENABLE\_ALL} graph optimisation was the only accelerator.

\subsection{Model Checksums (SHA-256, truncated)}

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Model file} & \textbf{SHA-256 (first 8 chars)} & \textbf{Size} \\
\midrule
\texttt{yolov8n.onnx} & \texttt{b3a1f2e4} & 12.3\,MB \\
\texttt{yolov8s.onnx} & \texttt{d7f2c8a1} & 42.8\,MB \\
\texttt{yolov8m.onnx} & \texttt{e9c4b3f7} & 99.0\,MB \\
\bottomrule
\end{tabular}
\end{center}

Full SHA-256 hashes and download links are in
\texttt{experiments/results/gpu\_summary.json} in the repository.

%% ---------------------------------------------------------------
\section{Measurement Protocol}

\begin{itemize}[leftmargin=*, noitemsep]
  \item \textbf{Input:} random FP32 tensor $1\times3\times640\times640$,
    values $\sim\mathcal{U}(0,1)$; batch size = 1.
  \item \textbf{Warmup:} 100 inference calls discarded before timing.
  \item \textbf{Timed runs:} 500 calls; wall-clock measured with
    \texttt{time.perf\_counter()} bracketing each \texttt{session.run()}.
  \item \textbf{Statistics:} p50, p95, p99 latency (ms);
    $\mathrm{FPS} = 1000 / \mathrm{p50}$; mean and std also recorded.
  \item \textbf{Threading:} \texttt{intra\_op\_num\_threads=8} for CPU
    provider; GPU provider uses the CUDA default stream.
  \item \textbf{Thermal steady-state:} 10-minute system idle before each
    run; GPU junction temperature remained below 83\,°C throughout.
  \item \textbf{Power:} GPU power sampled via
    \texttt{nvidia-smi -{}-query-gpu=power.draw -{}-format=csv}
    at 1\,s intervals during the 500-run window.
    GPU\,0 drew $\approx$68\,W (YOLOv8n CUDA) to
    $\approx$112\,W (YOLOv8m CUDA) during inference.
  \item \textbf{CPU isolation:} CPU runs used
    \texttt{CPUExecutionProvider} only; GPU was idle.
\end{itemize}

%% ---------------------------------------------------------------
\section{Pipeline-Timing Breakdown}

The same 500-run / 100-warmup protocol was applied to a sequential
five-stage pipeline:

\begin{enumerate}[leftmargin=*, noitemsep]
  \item \textbf{Preprocess:} BGR$\to$RGB, HWC$\to$BCHW,
    divide by 255, cast to FP32 (\texttt{numpy}).
  \item \textbf{Inference:} ONNX Runtime \texttt{session.run()}.
  \item \textbf{Postprocess:} confidence threshold filter ($> 0.25$),
    box decoding.
  \item \textbf{Tracking:} LITE-proxy association (Hungarian algorithm,
    IoU cost matrix, $\tau_\mathrm{IoU} = 0.45$).
  \item \textbf{Analytics:} zone occupancy count
    (point-in-polygon test).
\end{enumerate}

Each stage is timed separately; total pipeline latency = sum of all
five stages (Table~2 in the main article).

%% ---------------------------------------------------------------
\section{Confidence Intervals}

With 500 runs and empirical latency standard deviations of 2--5\,ms
(CUDA) and 3--12\,ms (CPU), the 95\% bootstrap confidence interval
on the p50 estimate is $\pm0.3$--$0.7$\,ms. Bootstrap formula:

\begin{equation}
\mathrm{CI}_{95} =
  \bigl[\hat{\theta}^*_{0.025},\;\hat{\theta}^*_{0.975}\bigr],
\end{equation}

where $\hat{\theta}^*_\alpha$ is the $\alpha$-quantile of the
bootstrap distribution of the median over $B = 10{,}000$ resamples.
Full distributions are in
\texttt{experiments/results/gpu\_summary.json}.

%% ---------------------------------------------------------------
\section{Reproducibility Checklist}

\begin{center}
\begin{tabular}{lc}
\toprule
\textbf{Artefact} & \textbf{Available in repo} \\
\midrule
Benchmark script (\texttt{run\_benchmarks\_gpu.py}) & \checkmark \\
Model export script (\texttt{export\_models.py})    & \checkmark \\
Environment spec (\texttt{setup\_env.sh})            & \checkmark \\
Raw CSV results (\texttt{experiments/results/})      & \checkmark \\
Bootstrap summary (\texttt{gpu\_summary.json})       & \checkmark \\
\bottomrule
\end{tabular}
\end{center}

\bigskip
\noindent Repository: \url{https://github.com/muxiddin19/EdgeVA}

\end{document}
